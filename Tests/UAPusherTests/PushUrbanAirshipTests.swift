import XCTest
@testable import UAPusher

class PushUrbanAirshipTests: XCTestCase {
    func test() {
        XCTAssertTrue(true)
    }

    static var allTests : [(String, (PushUrbanAirshipTests) -> () throws -> Void)] {
        return [
            ("test", test),
        ]
    }
}
